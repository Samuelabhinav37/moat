{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "forceEnabled": {
      "type": "boolean",
      "title": "Force protection on for every user in the organization."
    },
    "lockProtectionToggle": {
      "type": "boolean",
      "title": "Prevent users from turning protection off or pausing it on a site. Only locks the toggle -- set forceEnabled too if a user who already turned protection off should have it forced back on."
    },
    "lockFilterGroups": {
      "type": "boolean",
      "title": "Prevent users from changing which filter lists are enabled."
    },
    "managedFilterGroups": {
      "type": "object",
      "title": "Filter-list group slugs (see rules/manifest.json \"group\" values) mapped to forced on/off.",
      "additionalProperties": { "type": "boolean" }
    },
    "managedCustomBlockedDomains": {
      "type": "array",
      "title": "Domains to block for every user, in addition to whatever they've added themselves.",
      "items": { "type": "string" }
    },
    "athena": {
      "type": "object",
      "title": "Connects this install to a self-hosted Athena instance for minimized security-event reporting. Leave unset entirely for a normal/open-source install -- every field below is required for the integration to activate at all.",
      "additionalProperties": false,
      "properties": {
        "tenantId": { "type": "string", "title": "This org's tenant id in Athena." },
        "agentId": { "type": "string", "title": "The immutable Athena agent id returned during enrollment." },
        "bootstrapUrl": { "type": "string", "title": "Athena endpoint that exchanges bootstrapSecret for a short-lived session token." },
        "bootstrapSecret": { "type": "string", "title": "Shared secret provisioned by this org's Athena deployment, scoped to this org only." },
        "eventsUrl": { "type": "string", "title": "Athena endpoint that receives batched, minimized security events." },
        "policyUrl": { "type": "string", "title": "Optional. Athena endpoint serving a signed policy of org-blocked domains, beyond the bundled filter lists. Requires policyPublicKey too -- event reporting above works without either." },
        "policyPublicKey": {
          "type": "object",
          "title": "Optional, paired with policyUrl. An Ed25519 public key as a JWK, used to verify every fetched policy before it is trusted.",
          "additionalProperties": false,
          "properties": {
            "kty": { "type": "string" },
            "crv": { "type": "string" },
            "x": { "type": "string" }
          }
        }
      }
    }
  }
}
